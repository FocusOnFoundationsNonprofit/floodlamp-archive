Help me double check all markdown files in the root (meaning don't include files on the subfolders) of the following folder: [PASTE FOLDER PATH HERE]

Also, note that in the folder path the markdown files with filenames that begins with an underscore (_) are not included in the files to check.

What you will check is if there are issues on the markdown metadata and content, the metadata and content format is discussed in detail here:
@data/floodlamp/_prompts/prompt_convert-source-to-archive-md.md 

Also, note that there should be no text using heading 1 (#), if there is that should be replaced with ***INTERNAL TITLE:***

Now, for the main source where the markdown came from or where you will compare the content has the same file name as the md file, it could be in different formats (docx, xlsx, pdf), maybe it's easier to compare it with the easiest format for you (e.g. maybe the docx would be better to use than the pdf format).

Try to do the fix after checking, and for every change I should be able to do the review > keep or discard step.
